import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.monsteriptv.app',
  appName: 'MONSTER IPTV',
  webDir: 'dist/public',
};

export default config;
